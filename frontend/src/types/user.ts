export type User = {
  email: string;
  lastName: string;
  firstName: string;
  image: URL;
};

export type OAuthUser = {
  email: string;
  family_name: string;
  given_name: string;
  picture: URL;
};
